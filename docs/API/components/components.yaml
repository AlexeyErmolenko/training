schemas:
  $ref: ./schemas.yaml

parameters:
  $ref: ./parameters.yaml

responses:
  $ref: ./responses.yaml

securitySchemes:
  AuthToken:
    type: http
    scheme: bearer
    bearerFormat: JWT
  PasswordResetToken:
    type: apiKey
    in: query
    name: token
